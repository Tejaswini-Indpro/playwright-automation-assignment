{
  "name": "playwright-automation-assignment",
  "version": "1.0.0",
  "description": "Automation Anywhere Playwright Assignment",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "headed": "playwright test --headed",
    "debug": "playwright test --debug",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.1",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
